<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Linked Model Revisions"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   FontSize="14"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <!-- Data Grid -->
        <DataGrid x:Name="RevGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternationCount="2"
                  Margin="0,0,0,8">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="Padding" Value="2"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sequence" Binding="{Binding Sequence}" Width="80"/>
                <DataGridTextColumn Header="Revision Number" Binding="{Binding Number}" Width="160"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="140"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer: left = reload, right = copy/close -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>



            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CopyBtn" Content="Copy as TSV" Width="110" Height="28" Margin="0,0,8,0"/>
                <Button x:Name="CloseBtn" Content="Close" Width="80" Height="28"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
